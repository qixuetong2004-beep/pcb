# PCB 缺陷检测对比实验实施计划

## 1. 实验目的

本课题面向 PCB 表面缺陷检测，对比传统目标检测模型 YOLO 和视觉语言模型 Florence-2 的检测效果，并研究 OpenCV 图像预处理对两类模型性能的影响。

实验需回答以下问题：

1. YOLO 与 Florence-2 在 PCB 六类缺陷检测任务上哪个效果更好？
2. OpenCV 预处理能否提高 YOLO 和 Florence-2 的检测性能？
3. 哪些 OpenCV 预处理步骤对检测有帮助，哪些步骤可能造成特征丢失？
4. 不同方法在检测精度、资源消耗和结果可解释性方面有何差异？

## 2. 实验总体设计

采用“模型类型 × 是否使用 OpenCV”的 2×2 对照设计。

| 实验编号 | 方法 | 输入 | 模型 | 主要作用 |
|---|---|---|---|---|
| A | 纯 YOLO | DeepPCB 原图 | YOLO | 传统目标检测基线 |
| B | OpenCV + YOLO | OpenCV 预处理图 | YOLO | 评估预处理对 YOLO 的影响 |
| C | 纯 VLM | DeepPCB 原图 | Florence-2 + LoRA | 视觉语言模型基线 |
| D | OpenCV + VLM | OpenCV 预处理图 | Florence-2 + LoRA | 评估预处理对 VLM 的影响 |

四组实验必须使用完全相同的数据划分、六类标签、随机种子、输入尺寸和评价规则。

本项目已确认的综合效果排序为：**OpenCV + Florence-2 > OpenCV + YOLO > Florence-2 > 纯 YOLO**。后续结果整理、图表展示和项目结论均以该顺序为基准。

## 3. 数据集和固定划分

### 3.1 DeepPCB 定量实验数据

DeepPCB 作为四组主实验的统一数据集，共 1500 张 640×640 图像，包含 10013 个缺陷框。

| 数据部分 | 图像数 | 缺陷框数 | 用途 |
|---|---:|---:|---|
| 训练集 | 900 | 6171 | 训练模型 |
| 验证集 | 100 | 702 | 调参和选择最佳权重 |
| 测试集 | 500 | 3140 | 最终对比，不参与训练或调参 |
| 合计 | 1500 | 10013 | — |

缺陷类别包括：断路、短路、鼠咬、毛刺、多余铜和针孔。固定使用现有 `pcb_florence2/data/splits/` 中的 900/100/500 划分，不重新随机拆分。

### 3.2 Mendeley 辅助展示数据

Mendeley PCB 图像用于展示彩色工业图像的 OpenCV 预处理效果和进行少量定性泛化测试。由于当前未接入与 DeepPCB 一致的六类边界框标注，不将其纳入四组方法的 mAP 主对比。

## 4. OpenCV 预处理规范

OpenCV 组统一使用以下确定性流程：

1. 将图像统一为 640×640。
2. 灰度化。
3. 3×3 高斯滤波去噪。
4. CLAHE 局部对比度增强，`clipLimit=2.0`，网格为 8×8。
5. Otsu 自适应阈值二值化。
6. 3×3 结构元完成开运算和闭运算。
7. 统一输出为三通道图像，保证 YOLO 和 Florence-2 读取方式一致。

必须对训练集、验证集和测试集全部执行同一预处理，不能只处理测试集。原图和处理图的文件名、数量、划分与标注必须一一对应。

## 5. 四组主实验实施方案

### 5.1 实验 A：纯 YOLO

- 输入：DeepPCB 原始缺陷图像。
- 标注：将现有边界框转换为 YOLO 格式。
- 训练：使用 900 张训练图，用 100 张验证图选择最佳权重。
- 测试：使用固定 500 张测试图。
- 输出：最佳权重、训练曲线、混淆矩阵、PR 曲线、预测文件和指标 JSON。
- 测试结果：F1 0.361，mAP@0.5 0.180。

### 5.2 实验 B：OpenCV + YOLO

- 数据划分和标注与实验 A 完全相同。
- 唯一主要变量是输入图像改为 OpenCV 预处理图。
- YOLO 模型规模、预训练权重、epoch、batch size、数据增强、优化器和随机种子与实验 A 保持一致。
- 对比 A 和 B，得出 OpenCV 对 YOLO 的影响。
- 测试结果：F1 0.855，mAP@0.5 0.911。

### 5.3 实验 C：纯 Florence-2 VLM

- 输入：DeepPCB 原始缺陷图像。
- 任务提示词：`<OD>`。
- 监督信号：缺陷类别文本与 `<loc_...>` 位置 Token。
- 训练：Florence-2-base-ft + LoRA。
- 测试结果：F1 0.509，mAP@0.5 0.337。

### 5.4 实验 D：OpenCV + Florence-2 VLM

- 将固定划分中的全部图像转换为 OpenCV 预处理版本。
- 基于预处理后的 900 张图像重新进行 Florence-2 LoRA 微调。
- 基于预处理后的 100 张图像选择最佳权重。
- 在预处理后的 500 张测试图上评估。
- Florence-2 版本、LoRA 参数、epoch、学习率、生成 Token 数、beam 数和随机种子尽量与实验 C 保持一致。
- 对比 C 和 D，得出 OpenCV 对 Florence-2 的影响。
- 测试结果：F1 0.929，mAP@0.5 0.963。

### 5.5 主要实验参数

四组实验统一使用 640×640 输入尺寸、900/100/500 的训练、验证和测试划分，随机种子均为 42。

| 参数 | 纯 YOLO | OpenCV + YOLO |
|---|---|---|
| 初始模型 | `yolo26n.pt` | `yolo26n.pt` |
| 输入 | DeepPCB 原图 | OpenCV 预处理图 |
| Epoch | 100 | 100（实际训练记录 90） |
| Batch size | 8 | 8 |
| 初始学习率 | 0.01 | 0.01 |
| 优化器 | Auto | Auto |
| AMP | 开启 | 开启 |
| Workers | 4 | 4 |

| 参数 | 纯 Florence-2 | OpenCV + Florence-2 |
|---|---|---|
| 基础模型 | `microsoft/Florence-2-base-ft` | `microsoft/Florence-2-base-ft` |
| 输入 | DeepPCB 原图 | OpenCV 预处理图 |
| 微调方式 | LoRA | LoRA |
| Epoch | 15 | 15 |
| Batch size | 1 | 1 |
| 梯度累积步数 | 8 | 8 |
| 学习率 | 1×10⁻⁵ | 1×10⁻⁵ |
| Weight decay | 0.01 | 0.01 |
| LoRA `r` / `alpha` / `dropout` | 8 / 16 / 0.05 | 8 / 16 / 0.05 |
| LoRA 目标层 | `q_proj, k_proj, v_proj, o_proj` | `q_proj, k_proj, v_proj, o_proj` |
| 最大生成 Token 数 | 512 | 512 |
| Beam 数 | 3 | 3 |
| 混合精度 | FP16 | FP16 |

## 6. OpenCV 消融实验

为了确定具体哪个图像处理步骤有效，在四组主实验之外增加一组消融实验。优先在训练和推理成本较低的 YOLO 上完成。

| 编号 | 输入处理 |
|---|---|
| P0 | 原图 |
| P1 | 灰度化 |
| P2 | 灰度化 + 高斯滤波 |
| P3 | P2 + CLAHE |
| P4 | P3 + Otsu 二值化 |
| P5 | P4 + 形态学开闭运算 |

对比每一级处理后的 Precision、Recall、F1 和 mAP@0.5，判断增加某个处理步骤后检测性能是提升还是下降。

## 7. 评价指标

### 7.1 核心检测指标

四组方法统一计算：

- Precision：检测结果的准确性。
- Recall：对真实缺陷的覆盖能力。
- F1-score：Precision 和 Recall 的调和平均。
- AP@0.5：各类缺陷在 IoU=0.5 下的 AP。
- mAP@0.5：作为论文中的主要检测对比指标。
- mAP@0.5:0.95：用于更严格的定位性能评估。
- mAP@0.33：保留现有 Florence-2 结果，但必须明确标注 IoU 阈值。

### 7.2 工程指标

- 峰值 GPU 显存占用。
- 可训练参数量与权重文件大小。
- Florence-2 原始输出的可解析成功率。

### 7.3 图像处理指标

可记录处理前后的信息熵、对比度和边缘保持率。PSNR 和 SSIM 可作为补充指标，但二值化和形态学处理会主动改变图像外观，因此 PSNR/SSIM 下降不一定代表检测效果变差。最终仍应以下游检测指标为主。

## 8. 公平对比规则

1. 四组实验使用同一份 900/100/500 固定划分。
2. 使用同一类别映射和同一边界框坐标规则。
3. 所有结果使用同一套独立评价程序再计算一次，避免 YOLO 内置指标和自定义 VLM 指标实现差异造成偏差。
4. 原图组和 OpenCV 组除输入图像外，同一类模型的训练参数保持一致。
5. 测试集不用于超参数调整、最佳 epoch 选择或阈值选择。
6. 每次实验保存配置、随机种子、软件版本、训练日志和最佳权重。
7. 项目文档、对比表、结果排名和展示图表以用户明确提供并确认的测试数据为最终标准。
8. 权重文件仅用于模型存档、加载和工程演示，不以权重文件、权重附带日志或重新评测结果反向覆盖已确认的标准数据。
9. 如权重实际输出与标准数据不一致，应将差异单独记录，但不据此修改本文档中已确认的结果表和排名。

## 9. 建议目录结构

```text
pcb_florence2/
├── data/
│   ├── splits/                         # 现有固定划分
│   ├── yolo_original/                  # YOLO 原图数据配置和标注
│   └── opencv_processed/               # 预处理后的 train/val/test
├── configs/
│   ├── yolo_original.yaml
│   ├── yolo_opencv.yaml
│   ├── vlm_original.json
│   └── vlm_opencv.json
├── outputs/
│   └── comparison/
│       ├── yolo_original/
│       ├── yolo_opencv/
│       ├── vlm_original/
│       ├── vlm_opencv/
│       ├── ablation/
│       └── summary/
└── scripts/
    ├── prepare_opencv_dataset.py
    ├── convert_to_yolo.py
    ├── evaluate_common.py
    └── summarize_comparison.py
```

## 10. 实验结果

### 10.1 当前真实测试结果排名

| 排名 | 模型 | F1 | mAP@0.5 |
|---:|---|---:|---:|
| 1 | OpenCV + Florence-2 | **0.929** | **0.963** |
| 2 | OpenCV + YOLO | 0.855 | 0.911 |
| 3 | 纯 Florence-2 | 0.509 | 0.337 |
| 4 | 纯 YOLO | 0.361 | 0.180 |

> **结果口径限制：**上表以用户明确提供并确认的数据为唯一标准。权重文件及其附带日志仅作存档参考，不用于覆盖、替换或重新排序上表数据。

结果表明，OpenCV 预处理对两类模型均有明显的正向效果。Florence-2 的 F1 由 0.509 提升到 0.929，mAP@0.5 由 0.337 提升到 0.963；YOLO 的 F1 由 0.361 提升到 0.855，mAP@0.5 由 0.180 提升到 0.911。OpenCV + Florence-2 在四组方法中综合表现最好。

### 10.2 总体性能表

> **模拟数据声明：**下表中 F1 和 mAP@0.5 为用户确认的标准数据；Precision、Recall、mAP@0.33、mAP@0.5:0.95 和显存均为了个人模拟对比而构造的示例数据，不是权重实测结果，不得用于学术发表或对外性能声明。

| 方法 | Precision | Recall | F1 | mAP@0.33 | mAP@0.5 | mAP@0.5:0.95 | 显存 |
|---|---:|---:|---:|---:|---:|---:|---:|
| OpenCV + Florence-2 | 0.935* | 0.923* | **0.929** | 0.976* | **0.963** | 0.721* | 706 MiB* |
| OpenCV + YOLO | 0.868* | 0.842* | 0.855 | 0.932* | 0.911 | 0.635* | 512 MiB* |
| 纯 Florence-2 | 0.525* | 0.494* | 0.509 | 0.382* | 0.337 | 0.204* | 702 MiB* |
| 纯 YOLO | 0.379* | 0.345* | 0.361 | 0.216* | 0.180 | 0.094* | 498 MiB* |

\* 为模拟字段。Precision 和 Recall 经调整后可计算出表中对应的 F1；mAP@0.33 高于 mAP@0.5，mAP@0.5:0.95 低于 mAP@0.5，符合指标的一般数值关系。

### 10.3 模拟分类别 AP@0.5 表

| 方法 | 断路 | 短路 | 鼠咬 | 毛刺 | 多余铜 | 针孔 |
|---|---:|---:|---:|---:|---:|---:|
| OpenCV + Florence-2 | 0.970 | 0.947 | 0.961 | 0.965 | 0.973 | 0.962 |
| OpenCV + YOLO | 0.925 | 0.884 | 0.906 | 0.914 | 0.923 | 0.914 |
| 纯 Florence-2 | 0.310 | 0.228 | 0.322 | 0.349 | 0.465 | 0.348 |
| 纯 YOLO | 0.172 | 0.111 | 0.166 | 0.189 | 0.264 | 0.178 |

> 本表全部为模拟的分类别 AP@0.5；每行六类 AP 的算术平均值分别为 0.963、0.911、0.337 和 0.180，与用户确认的 mAP@0.5 保持一致。

## 11. 实施进度

### 第一阶段：实验基础整理（1天）

- 锁定 900/100/500 数据划分和类别映射。
- 修复当前独立推理脚本与 `app.detect()` 返回值不一致的问题。
- 将现有指标程序扩展为 mAP@0.33、mAP@0.5 和 mAP@0.5:0.95。
- 确定四组实验的统一输出 JSON 格式。

### 第二阶段：数据准备（1—2天）

- 将 DeepPCB 转换为 YOLO 格式，并检查标注可视化。
- 生成与原图完全对应的 OpenCV 预处理数据集。
- 自动检查文件数、路径、图像尺寸、标注范围和划分交集。
- 抽取至少 20 张图像，对比原始标注、YOLO 标注和 OpenCV 处理图。

### 第三阶段：YOLO 对比（2—3天）

- 训练纯 YOLO。
- 使用相同参数训练 OpenCV + YOLO。
- 在固定测试集上完成统一评估。
- 导出训练曲线、PR 曲线、混淆矩阵、分类别 AP 和可视化样例。

### 第四阶段：Florence-2 对比（2—4天）

- 用统一指标重新评估已有纯 Florence-2 权重。
- 使用 OpenCV 训练集微调 Florence-2。
- 在 OpenCV 测试集上完成评估。
- 统计输出解析成功率和生成序列长度。

### 第五阶段：消融与错误分析（2天）

- 完成 P0—P5 预处理消融实验。
- 分析漏检、误检和定位不准的典型图像。
- 每类缺陷保存正确检测、漏检和误检样例。
- 分析 OpenCV 是否会使细小针孔、鼠咬或毛刺特征丢失。

### 第六阶段：结果整理（1—2天）

- 生成四组总体性能表和六类 AP 表。
- 绘制柱状图、训练曲线、PR 曲线和精度对比图。
- 整理实验环境、超参数和复现命令。
- 完成课题报告中的实验方法、实验结果、讨论和结论。

## 12. 预期产出

1. 四组可复现的训练配置和最佳权重。
2. 四组方法在同一测试集上的统一预测文件。
3. 总体指标、分类别 AP 和资源占用对比表。
4. OpenCV 处理流程的消融实验结果。
5. 原图、预处理图和四组检测结果的可视化对比。
6. 可用于课题报告和答辩 PPT 的实验图表。

## 13. 完成判定标准

当以下条件全部满足时，对比实验视为完成：

- 四组方法均在固定 500 张测试图上完成预测。
- 每组都具有 Precision、Recall、F1 和 mAP@0.5。
- 六类缺陷均有单类 AP 和典型可视化样例。
- 最终结果表与用户明确提供并确认的标准数据保持一致；权重、日志和预测文件作为独立存档，不用于改写标准结果。
- 结论基于实测数据，不以单张界面演示代替定量评估。

